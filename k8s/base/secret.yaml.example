# Example secret file - DO NOT commit actual secrets to git!
#
# To create the actual secret, either:
# 1. Copy this to secret.yaml and replace values, then apply with kubectl
# 2. Create secret directly with kubectl:
#    kubectl create secret generic deckworthy-secrets \
#      --from-literal=STEAM_API_KEY=your-key \
#      --from-literal=ITAD_API_KEY=your-key \
#      -n deckworthy
#
# 3. Use Terraform to manage secrets (recommended)

apiVersion: v1
kind: Secret
metadata:
  name: deckworthy-secrets
  namespace: deckworthy
  labels:
    app: deckworthy
type: Opaque
stringData:
  # Get from https://steamcommunity.com/dev/apikey
  STEAM_API_KEY: "your-steam-api-key-here"

  # Get from https://isthereanydeal.com/dev/app/
  ITAD_API_KEY: "your-itad-api-key-here"
